# OP-Codex

Progetto per il One Piece Card Game.

## Agent skills

### Issue tracker

Issues live in Linear (team "Ribo Project", key `RIB`, project "OP-Codex"), accessed via the Linear MCP tools. See `docs/agents/issue-tracker.md`.

### Triage labels

Default five-role vocabulary (`needs-triage`, `needs-info`, `ready-for-agent`, `ready-for-human`, `wontfix`). See `docs/agents/triage-labels.md`.

### Domain docs

Single-context: one `CONTEXT.md` + `docs/adr/` at the repo root. See `docs/agents/domain.md`.
